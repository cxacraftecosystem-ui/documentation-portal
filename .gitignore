# Python
__pycache__/
*.py[cod]
.venv/
.env
*.egg-info/

# Node / Next.js
node_modules/
.next/
out/
dist/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local files
.DS_Store
Thumbs.db
*.local
*.log
.codegraph/
.agents/
skills-lock.json
2nd Workshop_Interview Questions.docx

# Terraform (never commit state — it can contain secrets — or local vars)
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl

# Keys / secrets
*.pem

# Playwright observation artifacts
frontend/pw-screens/
test-results/

# Android / Gradle
.gradle/
android/.gradle/
# Kotlin 2.x writes an incremental-compilation session cache here on every Gradle run.
android/.kotlin/
android/.idea/
android/app/build/
android/build/
local.properties

# Local-only transcription implementation reference (do not commit)
TRANSCRIPTION_SERVICE.md

# Downloaded boundary sources for scripts/build_boundaries.py — 63 MB of published GeoJSON that the
# script re-fetches with --fetch. The GENERATED assets under frontend/public/boundaries and
# android/.../res/raw ARE committed; these inputs are not.
.boundary-cache/
